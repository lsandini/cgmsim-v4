node_modules/
dist/
*.tsbuildinfo

# OS / editor
.DS_Store
*:Zone.Identifier
*.swp
*.swo
.vscode/
!.vscode/settings.json

# Environment
.env
.env.*

# Build artifacts
*.bak

# tsc compile output for UI — Vite bundles directly from .ts, these are dead artifacts
packages/ui/src/*.js
packages/ui/src/*.js.map

# Sourcemaps for the hand-maintained simulator runtime .js files (debug-only, not runtime)
packages/simulator/src/*.js.map

# Reference-only third-party source for study (e.g. Nightscout cgm-remote-monitor)
vendor/

# Surge deployment staging dir — local only
/deploy/

# Claude Code per-user settings (permissions, etc.) — not shared
.claude/settings.local.json
